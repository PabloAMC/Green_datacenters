# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Editor / OS / tooling
.DS_Store
.idea/
.vscode/
.claude/

# Note: figs/, output/, and scratch/ ARE tracked on purpose — the README and
# model_documentation.md reference the generated figures and the output/*_results.json
# export (the doc tables are regenerated from it via tools/regen_doc_tables.py).

# Ad-hoc CLI / sweep figures and outputs (regenerate on demand; keep only the
# canonical us_firm / eu_firm suite tracked)
figs/cli_*
output/cli_*
